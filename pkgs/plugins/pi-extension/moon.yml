---
$schema: "file://../../../.moon/cache/schemas/project.json"

language: typescript
layer: library

toolchains:
  default: ["javascript", "bun"]

tasks:
  typecheck:
    command: "bunx tsc --noEmit"
    inputs:
      - "package.json"
      - "tsconfig.json"
      - "src/**/*.ts"

  build:
    deps:
      - "~:typecheck"
    command: "noop"

  lint:
    deps:
      - "~:typecheck"
    command: "noop"

  test:
    command: "bun test"

  dev:
    script: |
      set -euo pipefail
      mkdir -p "$HOME/.pi/agent/extensions"
      ln -sfn "$PWD/" "$HOME/.pi/agent/extensions/zellij-agent"
    options:
      runInCI: false
